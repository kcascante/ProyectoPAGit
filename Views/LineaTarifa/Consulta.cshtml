@model IEnumerable<ProyectoPA.Models.LineaTarifa>

@{
    ViewBag.Title = "Consulta";
}

<h2>Consulta</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdLineaTarifa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdLinea)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdTarifa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdLineaTarifa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdLinea)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdTarifa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
